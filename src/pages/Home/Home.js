
// Frameworks functions
import React from 'react';
import './Home.scss';

export default function Home() {
    // const auth = useAuth();
    // El token ya lo tengo en el localStorage por que auth me muestra 2 funciones y el token
    // ahora con jwt-decode, cifraremos el token para mostrarlo en consola.
    // console.log(auth);
    return (
        <div>
            <h1> estamos es Home </h1>
        </div>
    )
}
